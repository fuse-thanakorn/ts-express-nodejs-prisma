import Joi from 'joi';

const getPostCodes = {
  query: Joi.object().keys({

  })
};
const getPostCodeById = {
  query: Joi.object().keys({

  })
};

export default {

  getPostCodes,
  getPostCodeById
};
